# CODEOWNERS — used for automatic review assignment on PRs.
# GitHub matches these paths to actual GitHub user/team handles. The
# previous file referenced @alexmayhew (a non-existent handle on this
# repo) and silently matched nothing — fixed to the real owner.

# Default owners for everything in the repo
* @LecoMV

# Frontend components
/src/components/ @LecoMV

# Configuration files
*.config.* @LecoMV
.github/ @LecoMV

# SEO Audit — alexmayhew.dev — 2026-04-16

**Auditor:** seo-auditor agent
**Scope:** All routes in `src/app/`, sitemap, robots, metadata, canonicals, internal linking, schema, pSEO
**Prior context:** Near-zero domain authority (11/180 pages indexed), recent sitemap prune to ~126 pages, +73 internal links added

---

## Summary

42 findings: **6 critical, 14 high, 13 medium, 9 low**. The biggest levers are (1) draft-status newsletters leaking into the sitemap/site as thin content, (2) blog articles with zero in-content internal linking to related posts (huge hub-spoke wasted equity), (3) several missing OG images that 404, (4) `/tools/saas-readiness` missing from sitemap, (5) demo/offline/docs thin-content pages indexable, and (6) pervasively long meta descriptions that Google will truncate. Core infrastructure (robots.txt, JSON-LD, hreflang N/A, canonical coverage) is solid.

---

## CRITICAL (blocks indexing/rankings or creates Google trust signals)

### C1. All 36 newsletter issues are `status: "draft"` but rendered live

- **File:** `content/newsletter/issues/*.mdx` (36 files, all `status: "draft"`); `src/app/newsletter/[slug]/page.tsx:20-21` (no status filter in `generateStaticParams`); `src/app/sitemap.ts:182-189` (filters only by `publishedAt <= new Date()`, not status)
- **Evidence:** `grep ^status: content/newsletter/issues/*.mdx | sort | uniq -c` → `36 status: "draft"`. 11 have `sendDate <= 2026-04-16`, so 11 draft newsletters are in the sitemap and indexable.
- **Impact:** Google is being fed 11+ "live" pages that are internally flagged as drafts. Each newsletter page is essentially a re-format of content that may also exist on other channels (Listmonk/email). For a near-zero-authority site, indexing 11 unfinished pages compounds the "low-quality site" signal and wastes crawl budget.
- **Fix:** Filter on `issue.status === "sent"` (or `"scheduled"`) in (a) sitemap generator, (b) `/newsletter` archive list, (c) `/newsletter/[slug]` `generateStaticParams`. Alternative: keep them but add `robots: { index: false, follow: true }` to each newsletter detail until marked `sent`.

### C2. Missing Open Graph images cause 404s on social shares

- **File:** `src/app/tools/traceforge/page.tsx:34` (`/og-traceforge.png`), `src/app/tools/pilot/page.tsx:31` (`/og-pilot.png`), `src/app/tools/voice-cloner/page.tsx:57` (`/og-voice-cloner.png`)
- **Evidence:** `ls public/og-*.png` → only `og-image.png` exists. The three referenced tool-specific OG images do not exist in `public/`.
- **Impact:** When these pages are shared on LinkedIn/X/Slack/Discord, the preview falls back to a broken image or no image. Social shares are one of the few ways this near-zero-authority site can earn attention. Broken previews kill CTR and look unprofessional.
- **Fix:** Either (a) generate and commit the three PNGs to `public/`, or (b) switch these three files to use the dynamic `/og?title=...&category=...` route that works for every other page on the site.

### C3. `/tools/saas-readiness` missing from sitemap

- **File:** `src/app/sitemap.ts:70-94` (tools section lists only 3 tools)
- **Evidence:** `src/app/tools/saas-readiness/page.tsx` exists and is a published lead magnet with full metadata, but it is not referenced in `sitemap.ts`. Grep for `saas-readiness` in `sitemap.ts` returns zero hits.
- **Impact:** This is a 2026-04-02 lead magnet (per memory). Not being in the sitemap means Googlebot relies entirely on internal links to discover it. On a zero-authority site that is already crawl-starved, omission from the sitemap directly delays indexing by weeks/months.
- **Fix:** Add entry in `sitemap.ts` static pages array: `{ url: \`${siteUrl}/tools/saas-readiness\`, lastModified: siteLastUpdated, changeFrequency: "monthly", priority: 0.9 }`.

### C4. Blog articles have NO in-content related-posts internal linking

- **File:** `src/components/blog/blog-article.tsx` (no `RelatedBlogPostsSection` import/usage), `src/app/blog/[slug]/page.tsx` (passes only article markup)
- **Evidence:** `RelatedBlogPostsSection` is used ONLY on the four pSEO route types (services, migrations, integrations, comparisons). `grep -l "RelatedBlogPostsSection" src/app/blog/` → empty.
- **Impact:** 74 blog posts × zero related-post links in-article = zero hub-spoke equity flow between posts. Hubs don't pass authority to spokes. Spokes don't link laterally. The 5 hub/39 spoke architecture is invisible to Google except via the `/blog` listing (which is flat and alphabetical, not clustered). This is the single largest missed-internal-linking opportunity on the site.
- **Fix:** Add `RelatedBlogPostsSection` (or a new `SeriesSidebar`) to `BlogArticle` that shows (a) other posts in the same `series`, (b) the series hub, (c) 2-3 cross-series related posts. Wire from `blog/[slug]/page.tsx` using `post.series` to query `blog` index.

### C5. `/docs` indexed and in sitemap despite having only a stub index page

- **File:** `src/app/sitemap.ts:64-69` (`/docs` priority 0.6); `content/docs/` contains only `index.mdx` (23 lines of placeholder copy)
- **Evidence:** `cat content/docs/index.mdx` is 23 lines of generic "Welcome to the documentation hub…" with no actual docs. `cat content/docs/meta.json` shows sections exist but point at only the `index` page.
- **Impact:** This is a textbook "thin content" page. Google's SpamBrain and quality classifier flag sites where header nav links to near-empty sections. On a zero-authority domain, any "thin content" signal is amplified.
- **Fix:** Either (a) remove `/docs` from sitemap and add `noindex` until real docs exist, or (b) remove the footer link + nav entry and 410 the route. Recommend (a): `robots: { index: false, follow: true }` on `/docs` and drop from sitemap until there are ≥3 substantial docs pages.

### C6. `/offline` and `/demo*` pages indexable with no metadata

- **File:** `src/app/offline/page.tsx` (no `metadata` export), `src/app/demo/page.tsx` (no `metadata`), `src/app/demo/backgrounds/page.tsx` (no `metadata`)
- **Evidence:** These pages inherit the root `layout.tsx` defaults (`index: true, follow: true`) and use the default title "Alex Mayhew | Technical Advisor & Systems Architect". `robots.ts:9` does disallow `/demo/` but does NOT disallow `/offline`, and meta-robots would still override the robots.txt hint for indexation if Google already crawled.
- **Impact:** `/offline` is a PWA fallback with 24 lines of content — pure thin content, AND it has the default homepage title, creating a duplicate-title signal. `/demo/*` pages use the homepage title too.
- **Fix:** Export `metadata = { robots: { index: false, follow: true }, title: "Offline", alternates: { canonical: "/offline" } }` on `/offline`. Add same to `/demo/page.tsx` and `/demo/backgrounds/page.tsx` (belt-and-suspenders with the robots.txt disallow). Also add `/offline` to `disallow` in `robots.ts`.

---

## HIGH (significant ranking/CTR impact)

### H1. Blog descriptions massively exceed 160 chars (Google truncates at ~158-165)

- **File:** 60+ posts in `content/blog/*.mdx`
- **Evidence:** Sample: `caching-strategies-production` = 240 chars, `code-review-practices-scale` = 243 chars, `edge-computing-saas` = 225 chars, `hiring-ai-engineers` = 225 chars, `agentic-engineering` = 214 chars, `ai-assisted-development-reality` = 211 chars. At least 40+ posts exceed 180 chars.
- **Impact:** Google truncates the displayed SERP snippet, meaning the most important CTA/keyword at the end of the description is cut off. Lower CTR on the 30 currently-indexed pages. Also harms AI-engine citation quality (SGE, Perplexity use meta description heavily).
- **Fix:** Tighten all blog descriptions to 140-160 chars. Priority: posts currently getting impressions in GSC, then hubs, then recent posts, then remainder.

### H2. Blog titles that push past 60 chars including brand suffix

- **File:** `content/blog/*.mdx` — `src/app/blog/[slug]/page.tsx:37` uses `title: post.title` which gets the ` | Alex Mayhew` suffix via `layout.tsx:59` template
- **Evidence:** `the-metr-paradox` title is 97 chars before suffix = 111 chars with `| Alex Mayhew`. 7+ other posts exceed 60 chars bare: `building-ai-features-users-want` (77), `ai-assisted-development-reality` (75), `building-production-mcp-servers` (75), `prompt-engineering-for-developers` (74), `ai-code-review` (73).
- **Impact:** Titles >60 chars truncate in SERP. Pixel-width rules (≈ 580px) mean anything past "Characters" in "Agentic Engineering: What Changes When Developers Become Orc…" gets eaten. This also dilutes primary-keyword prominence (the keyword pushed past char 60 loses CTR weight).
- **Fix:** Review each >60-char title. Shorten by dropping subtitle clauses. Or use Next `title.absolute` / `title.template: null` on individual posts to strip the brand suffix when the title is already long.

### H3. Newsletter detail page metadata inherits noop description

- **File:** `src/app/newsletter/[slug]/page.tsx:31-32`
- **Evidence:** `description: issue.subject` — the `subject` field is the email subject line (e.g. "Why your SaaS needs a monolith (yes, really)"), not a meta description. No OG image, no Twitter card image.
- **Impact:** If newsletter pages do get indexed (once drafts become published per C1), each has a terse email-subject description and no social preview image. Low CTR, poor preview.
- **Fix:** Add a proper `description` field (100-160 chars) to newsletter frontmatter. Fall back to `subject` only if absent. Add OG image via the `/og` route: `\`/og?title=...&category=Newsletter\``.

### H4. Homepage `title` is untemplated — bloats to 60+ chars

- **File:** `src/app/layout.tsx:58`
- **Evidence:** `default: "Alex Mayhew | Technical Advisor & Systems Architect"` = 57 chars. This is the homepage title.
- **Impact:** OK on character count, but the primary keyword "Technical Advisor" sits at char 14 — acceptable but not optimal. More importantly, the homepage does not have its own `title` override (`src/app/page.tsx:5-9` only sets canonical), so it inherits this default on the homepage too. Competing for "technical advisor" requires front-loading.
- **Fix:** Consider setting homepage `title.absolute: "Technical Advisor for SaaS Founders | Alex Mayhew"` — front-loads the keyword, keeps brand, 52 chars.

### H5. `/technologies/[slug]` description uses truncated raw copy

- **File:** `src/app/technologies/[slug]/page.tsx:32`
- **Evidence:** `` description = `Expert ${technology.displayName} development and consulting. ${technology.expertiseLevel.slice(0, 120)}...` ``. For React: produces "…Production experience since Next.js 9 (2019), migrated multiple codebases through Pages Router to App Router. Deep unders…" — that is not a meta description, that's a sentence fragment with visible "…".
- **Impact:** Ugly SERP snippet with mid-word truncation. Loses SERP CTR. Also looks like lazy templating to Google.
- **Fix:** Add a dedicated `seo.description` field to each Technology object (100-160 char hand-written), fallback to a sentence generator that doesn't mid-word-truncate.

### H6. `hello-world.mdx` is orphan blog post (no series, no isHub)

- **File:** `content/blog/hello-world.mdx`
- **Evidence:** `grep -L "series:" content/blog/*.mdx | grep -v -f <(grep -l "isHub: true" ...)` → `content/blog/hello-world.mdx`. No series key, no isHub flag.
- **Impact:** 73 of 74 blog posts are in the hub-spoke structure; this one post is completely orphan. It would receive no internal links from series siblings.
- **Fix:** Either (a) add a relevant series (probably `frontend-architecture` given its content about atmospheric engineering), or (b) delete if it's a remnant starter post. Recommend (b) unless it has backlinks.

### H7. pSEO description truncation — 3 of 19 exceed 160

- **File:** `src/data/pseo/pages.ts`
- **Evidence:** Measured lengths: `next-js-for-saas` (160), `legacy-migration` (161), `performance-optimization` (163), `technical-due-diligence` (168). Plus multiple migration descriptions at 175-180 chars (`src/data/pseo/migrations.ts`).
- **Impact:** SERP truncation on the most commercial-intent pages on the site. These are the pages trying to drive actual leads — truncated descriptions kill the CTA.
- **Fix:** Tighten all to ≤158 chars. Each description should end with a CTA ("Free audit", "Free assessment", etc.) — preserve that CTA when trimming.

### H8. Comparison page titles exceed 60 chars

- **File:** `src/data/pseo/comparisons.ts`
- **Evidence:** `postgresql-vs-mongodb-for-fintech` title = 61 chars, `stripe-vs-adyen-for-fintech` = 61 chars, `aws-vs-cloudflare-for-startups` = 66 chars. These are set as `page.seo.title` in `src/app/services/comparisons/[slug]/page.tsx:47` and have no site-brand suffix added, so the 61-66 is the final length.
- **Impact:** Truncation in SERP. Comparison pages currently noindexed (`robots: { index: false }` on all three verticals) so impact is deferred — but once they are unblocked post-authority-build, these will truncate.
- **Fix:** Shorten comparison titles. Drop "2026" year or use `"2026 Guide"` prefix on description instead of title. Target 48-55 chars.

### H9. Migration page titles consistently include redundant " | Technical Advisor"

- **File:** `src/data/pseo/migrations.ts`
- **Evidence:** Every migration seo.title ends `… Migration | Technical Advisor`. `jquery-to-react-migration` = 45 chars, `angularjs-to-nextjs-migration` = 50 chars. Integration titles do same pattern. All under 60 which is fine, but "| Technical Advisor" consumes ~20 chars of keyword real estate every title.
- **Impact:** Google ignores duplicated brand suffix patterns, but wastes CTR-driving pixel space. On page-type pages you typically win more CTR with outcome keywords (e.g. "Zero Downtime Migration", "6-Week Playbook") than brand.
- **Fix:** Replace "| Technical Advisor" with outcome-driven modifier per page. Examples: `"jQuery to React Migration | Zero-Downtime Playbook"`, `"AngularJS to Next.js Migration | 6-Week Timeline"`.

### H10. `LocalBusinessJsonLd` is a no-op stub still imported in layout

- **File:** `src/components/seo/local-business-json-ld.tsx` (returns `null`), `src/app/layout.tsx:145`
- **Evidence:** The component file is a comment saying it's been merged into `JsonLd` but the import and JSX invocation remain.
- **Impact:** Harmless but is dead code in the critical render path on every page. Not an SEO issue per se, but adds to the "component bloat" problem and confuses future engineers.
- **Fix:** Remove import + JSX call from `layout.tsx`. Delete the file.

### H11. `llms.txt` route caps at 20 spoke posts — drops 54 articles

- **File:** `src/app/llms.txt/route.ts:23` (`spokes.slice(0, 20)`)
- **Evidence:** `publishedPosts.filter(!isHub).slice(0, 20)` — 74 - 5 hubs = 69 spokes, only 20 listed.
- **Impact:** AI crawlers (GPTBot, ClaudeBot, PerplexityBot etc. which are allowed per `robots.ts`) consuming llms.txt see only 20 of 69 articles. Most AI citation surfaces use llms.txt as the authoritative index for recent/important content. You're hiding 71% of your corpus from AI engines that ARE crawling you.
- **Fix:** Remove the `.slice(0, 20)` — AI engines can handle a large llms.txt (commonly 10-50KB). With 74 posts the llms.txt would be ~15KB which is fine.

### H12. Sitemap `changeFrequency` and `priority` are purely advisory but may signal spam if inconsistent with real update cadence

- **File:** `src/app/sitemap.ts:62` (`changeFrequency: "weekly"` for `/blog`), line 115 (`changeFrequency: "monthly"` for blog posts), line 56 (`priority: 0.95` for `/services`)
- **Evidence:** Most static pages use `"monthly"`, blog listing is `"weekly"`. Blog actually updates every ~2 weeks (2 posts/month per CLAUDE.md), so `weekly` slightly overpromises. Priorities 0.95 on `/services` and 0.9 on `/work` are arbitrary — Google ignores values but inconsistency between your declared priority and actual internal-link count can be noise.
- **Impact:** Minor. Google largely ignores these fields since 2023 but over-declared `priority` on every page (0.85+ average) is self-defeating — if everything is priority 0.9, priority loses signal.
- **Fix:** Flatten priorities: homepage 1.0, hubs (services, blog, work, for) 0.9, leaves 0.7, legal 0.3. Blog `changeFrequency: "monthly"` matches the 2/month cadence.

### H13. Fumadocs `/docs` page description is 60-char one-liner

- **File:** `content/docs/index.mdx:3`
- **Evidence:** `description: Technical documentation and guides for projects and tools.` — 58 chars. No OG image override, generic.
- **Impact:** If `/docs` stays indexable per C5 (not recommended), its SERP snippet is barebones.
- **Fix:** Either remove per C5 or expand the page with real content + 140-char description.

### H14. Schema `Person.knowsAbout` has 25 entries but no corresponding hasCredential/award linkages

- **File:** `src/components/seo/json-ld.tsx:22-54`
- **Evidence:** 25-entry `knowsAbout` array mixed with 4-entry `award`. Person schema has no `hasCredential`, no `alumniOf`, no `memberOf`.
- **Impact:** E-E-A-T signal gap. For a "technical advisor" Person entity on a zero-authority domain, Google heavily uses structured data for author E-E-A-T. Empty credentials reduce knowledge-graph matching to real LinkedIn/X entity.
- **Fix:** Add `hasCredential`, `alumniOf` (if applicable), `memberOf` (professional orgs), and `worksFor` should reference a real `Organization` with LinkedIn `sameAs`. See `docs/research/person-schema-eeat-2026.md` (cited in MEMORY.md).

---

## MEDIUM (optimization opportunities)

### M1. Twitter card `creator` only set on blog/pSEO pages, missing on hubs

- **File:** `src/app/about/page.tsx`, `src/app/work/page.tsx`, `src/app/contact/page.tsx`, `src/app/services/page.tsx`, `src/app/for/page.tsx`, `src/app/newsletter/page.tsx`, `src/app/tools/page.tsx`, `src/app/tools/pilot/page.tsx`, `src/app/tools/traceforge/page.tsx`, `src/app/tools/voice-cloner/page.tsx`, `src/app/tools/saas-readiness/page.tsx`
- **Evidence:** These pages set `twitter.card` but omit `creator: "@alexmayhewdev"`. Only blog/services/migrations/integrations/comparisons/roles/technologies include creator.
- **Impact:** Twitter cards show "via @alexmayhewdev" when creator is set — drives follows and earned exposure. Missing from 11+ hub pages.
- **Fix:** Add `creator: "@alexmayhewdev"` and `site: "@alexmayhewdev"` to all `twitter` blocks. Consider extracting shared twitter config to avoid duplication.

### M2. `ItemList` schema on services hub lacks `item.@id` consistency

- **File:** `src/app/services/page.tsx:50-62`
- **Evidence:** Each item.item.`@type: "Service"` uses a bare `url` but no `@id`. The service detail page's `ServiceJsonLd` component likely creates a different `@id` for the same service — referential mismatch.
- **Impact:** Google can't reliably connect the hub `ItemList` entries to the detail page Service entities. Loses knowledge-graph linkage.
- **Fix:** Add `"@id": \`${siteUrl}/services/${page.slug}#service\``to each item, and ensure`ServiceJsonLd`uses the same`@id` on detail pages.

### M3. Newsletter archive lists all 36 issues including drafts

- **File:** `src/app/newsletter/page.tsx:40-49` (no status filter)
- **Evidence:** `newsletter.sort(...)` with no filter — all 36 issues shown, including ones with `status: "draft"` and `sendDate` in the future.
- **Impact:** Visitors click in and see unfinished content. Googlebot also discovers all 36 URLs via this archive page, bypassing any sitemap gating.
- **Fix:** Filter `newsletter.filter(issue => issue.status === "sent")` in the archive page.

### M4. `generateMetadata` returns `{}` on 404 paths — no noindex

- **File:** `src/app/blog/[slug]/page.tsx:32`, `src/app/services/[slug]/page.tsx:42-44`, migrations/integrations/comparisons (same pattern), `src/app/newsletter/[slug]/page.tsx:27`
- **Evidence:** When slug lookup fails, returns `{}` then the default page 404s. But `{}` means default title/description from layout — brief race condition where crawlers may see default metadata on a 404'd URL.
- **Impact:** Minimal with `dynamicParams = false` (which blocks unknown slugs at build). But newsletter uses `dynamicParams` default (true), so unknown newsletter slugs render default layout metadata before `notFound()` resolves.
- **Fix:** Return `{ robots: { index: false }, title: "Not Found" }` when slug not found. Set `dynamicParams = false` on newsletter `[slug]` to match blog/services.

### M5. pSEO page OG images use long raw SEO title + description combo

- **File:** `src/app/services/[slug]/page.tsx:50`, migrations/integrations/comparisons same pattern
- **Evidence:** `\`/og?title=${encodeURIComponent(page.seo.title)}&description=${encodeURIComponent(page.seo.description)}&category=Service\``— pushes 150+ char description into OG template. The`/og` route clips at 120 chars with ellipsis (`route.tsx:118`), so visible OG description is truncated.
- **Impact:** OG previews on LinkedIn/Slack/Discord show `…` mid-word. Poor social CTR.
- **Fix:** Add a dedicated `page.seo.ogDescription` (≤100 chars) to pSEO data, fall back to sliced seo.description only if absent. Or expand the `/og` template to two-line wrap.

### M6. No BreadcrumbList JSON-LD on pSEO service/migration/integration/comparison detail pages

- **File:** `src/app/services/[slug]/page.tsx`, `src/app/services/migrations/[slug]/page.tsx`, `src/app/services/integrations/[slug]/page.tsx`, `src/app/services/comparisons/[slug]/page.tsx`
- **Evidence:** Blog posts emit `BreadcrumbJsonLd` but pSEO detail pages do not. Services hub has breadcrumbs; detail pages don't.
- **Impact:** Misses a Google rich-result opportunity (breadcrumb trail in SERP). Also weakens the site-depth signal that breadcrumbs convey to crawlers.
- **Fix:** Add `BreadcrumbJsonLd` to each pSEO detail page. For services: `Home → Services → {Service Name}`. For migrations: `Home → Services → Migrations → {…}`.

### M7. `/for` nav item is labeled "Advisory" not "For Leaders" — canonical ambiguity

- **File:** `src/components/ui/navigation.tsx:31`, `src/app/for/page.tsx:79` (breadcrumb says "For Leaders")
- **Evidence:** Nav shows "Advisory", page breadcrumb schema names it "For Leaders", page title is "Technical Advisory by Role".
- **Impact:** Minor internal-naming inconsistency. Users click "Advisory" expecting a different page concept than "For Leaders".
- **Fix:** Pick one: rename nav to "For" or "Roles", or align breadcrumb+title to "Advisory". Recommend changing nav to "For" (matches URL slug) and page heading "Advisory for Your Role".

### M8. Person schema `sameAs` missing Bluesky (present in footer)

- **File:** `src/components/seo/json-ld.tsx:9-13` (Person sameAs), vs `src/components/ui/footer.tsx:32` (footer includes Bluesky)
- **Evidence:** Person entity sameAs lists GitHub, LinkedIn, X, Bluesky — OK. But `organizationSchema.sameAs` (lines 76-81) drops Bluesky. The inconsistency is internal but signals to Google that the two entities are disconnected.
- **Impact:** Minor. Organization sameAs is less load-bearing than Person's.
- **Fix:** Add Bluesky to `organizationSchema.sameAs` for consistency. Consider also adding GitHub org URL if there's a dedicated org/company profile.

### M9. No `FAQPage` JSON-LD on `/for/[role]` detail pages despite having FAQ data

- **File:** `src/app/for/[role]/page.tsx` (imports `RoleJsonLd`); `src/components/seo/role-json-ld.tsx`
- **Evidence:** Role pages include `faqs` in data (per types.ts `RoleFaqItem`) but Grep `FaqJsonLd` in role page → only used for blog hub pages.
- **Impact:** Missing Google FAQ rich-result eligibility on high-commercial-intent pages.
- **Fix:** Emit `FaqJsonLd` from role pages when `page.faqs.length > 0`. Same audit needed for service/migration/integration/comparison pages — if they have `faqs` in data, they should emit.

### M10. Priorities on non-commercial pages slightly higher than ideal

- **File:** `src/app/sitemap.ts:54-56` (services = 0.95), line 77 (/tools/traceforge = 0.95), line 62 (/blog = 0.9 but hub posts also 0.9)
- **Evidence:** Too many pages at ≥0.9 priority (12+). Homepage is 1.0.
- **Impact:** Google ignores absolute values but uses relative hierarchy. With everything at 0.85-0.95, you've flattened the signal.
- **Fix:** Tier: homepage 1.0 / top hubs (services, blog, work, for) 0.9 / detail pages 0.7 / legal 0.3. Tools individual pages should be 0.7 not 0.9/0.95.

### M11. `robots.ts` missing `/keystatic/*` edge cases

- **File:** `src/app/robots.ts:9`
- **Evidence:** Disallows `/keystatic/` (trailing slash). Users requesting `/keystatic` (no slash) may still hit 404 via layout check, but robots.txt won't explicitly disallow.
- **Impact:** Minor. Since layout returns `notFound()` in prod, Googlebot will see 404. But 404s on crawl waste budget.
- **Fix:** Disallow both `/keystatic` and `/keystatic/`.

### M12. No `max-image-preview:large` fallback for non-Google bots

- **File:** `src/app/layout.tsx:82-88`
- **Evidence:** `robots.googleBot` has `max-image-preview: large` but `robots.index/follow` for generic bots lack it. Bing/DuckDuckGo honor the general directive.
- **Impact:** Non-Google SERPs may show smaller image thumbnails.
- **Fix:** Hoist `max-image-preview: large` to the generic `robots` object (Next.js will write to the meta tag not just googlebot-specific).

### M13. Blog listing page has no server-rendered pagination, only `BlogPostNav` all-list

- **File:** `src/app/blog/page.tsx:107-131`
- **Evidence:** `BlogPostNav` lists all 74 posts server-rendered (good) but there's no `/blog/page/2` paginated URL structure.
- **Impact:** This is a deliberate choice — see `docs/research/blog-pagination-seo-nextjs15-2026.md` ("Googlebot doesn't click Load More" — server-rendered full list solves it). Current implementation is correct. Flagging only to confirm the research was applied.
- **Fix:** No action needed. Verified correct.

---

## LOW (polish)

### L1. Homepage meta description lacks CTA

- **File:** `src/app/layout.tsx:61-62`
- **Evidence:** "Technical advisor architecting production systems that scale from MVP to millions. Strategic guidance for startups where tech decisions have business consequences." 163 chars, no CTA verb.
- **Fix:** Tighten + add soft CTA: "Technical advisor architecting production systems that scale from MVP to millions. Book a strategy call for tech decisions with business consequences." (≤155 chars).

### L2. `alternates.canonical` uses absolute URL in some places, relative in others

- **File:** Relative: `src/app/page.tsx:7`, `src/app/about/page.tsx:32`, `src/app/blog/page.tsx:31`. Absolute: `src/app/services/[slug]/page.tsx:80` (`pageUrl` absolute), services/migrations/integrations/comparisons detail pages.
- **Impact:** Next.js resolves both correctly via `metadataBase`. Functional no-op but inconsistent codebase style.
- **Fix:** Pick one. Recommend relative (shorter, DRY, Next resolves). Replace absolute URLs in the 4 pSEO page files.

### L3. No `priority: 1.0` explicit on any page (homepage uses `1`)

- **File:** `src/app/sitemap.ts:31`
- **Evidence:** `priority: 1` — numerically equivalent to 1.0. Not an issue.
- **Fix:** None.

### L4. RSS feed `ttl` not set — some aggregators may hit every minute

- **File:** `src/app/feed.xml/route.ts` (partial view, need to verify)
- **Impact:** Minor bandwidth/CDN cost. Not an SEO issue.
- **Fix:** Add `<ttl>60</ttl>` in channel.

### L5. `og:locale` hardcoded to `en_US` despite serving `en-US` audiences globally

- **File:** `src/app/layout.tsx:102`
- **Impact:** OK. Not an issue unless you expand to en-GB/en-AU.
- **Fix:** None right now. Add `og:locale:alternate` if ever adding locales.

### L6. Service page titles with "|" separator vary pattern

- **File:** `src/data/pseo/pages.ts` — titles like `"Next.js for SaaS | 99.95% Uptime"` vs migration titles `"jQuery to React Migration | Technical Advisor"`.
- **Impact:** Inconsistency across page types. Minor.
- **Fix:** Document title pattern in pSEO rules: `{Tech} for {Industry} | {Outcome}` for services; `{From} to {To} Migration | {Outcome}` for migrations. Enforce in validation.

### L7. No `aggregateRating` or `review` on Service schema

- **File:** `src/components/seo/service-json-ld.tsx` (not read, inferred from `ServiceJsonLd` usage)
- **Impact:** Missing Star-rating rich-result opportunity. Requires actual testimonials.
- **Fix:** Gather and display ≥3 real reviews per Service, then emit `AggregateRating` in schema.

### L8. `sitemap.ts` computes `siteLastUpdated` from NEXT_PUBLIC_BUILD_TIME but caches fresh on every build

- **File:** `src/app/sitemap.ts:24`
- **Evidence:** Every build gets new timestamp. Static page `lastModified` thus jitters on every deploy even when content didn't change. Google flagged this in prior research as "distrust signal" — MEMORY.md mentions "fixed dates to avoid Google distrust".
- **Impact:** Builds happen ~3x/week. Each build updates lastmod on every static page, defeating the "fixed dates" intent.
- **Fix:** Hard-code `siteLastUpdated` to a manual release date (e.g., `new Date("2026-04-05")`) and only bump when content materially changes. Remove the NEXT_PUBLIC_BUILD_TIME dependency for sitemap lastModified.

### L9. `/tools/pilot` and `/tools/voice-cloner` have no canonical difference signal despite being conceptually different offerings

- **File:** `src/app/tools/pilot/page.tsx:45` (`canonical: "/tools/pilot"`), `src/app/tools/voice-cloner/page.tsx:71`
- **Evidence:** Both canonicals point at their own page. Canonical is correct. No issue.
- **Fix:** None.

---

## Crawlability Quick-Verification (what's right)

- `robots.ts` correctly allows all major AI crawlers (GPTBot, ClaudeBot, PerplexityBot, Google-Extended)
- Sitemap reference present in robots.txt
- `metadataBase` correctly set to `https://alexmayhew.dev` in `src/app/layout.tsx:56`
- Canonical coverage is ~95% of routes (homepage, all static, all dynamic generateMetadata paths)
- Comparison/Integration/Migration pages correctly `noindex, follow`
- `/keystatic/` correctly disallowed AND 404ed in prod
- JSON-LD Person/Organization/WebSite/ConsultingService correctly linked via `@id` references
- OG `/og` dynamic route exists and renders per-page images for blog and pSEO
- No JavaScript-dependent critical content (all pages are RSC or have server-rendered HTML)
- `hreflang` correctly N/A (English-only)
- Featured blog images: 74/74 posts have `/public/images/blog/{slug}-featured.webp` (verified via file count match)

---

## Recommended Fix Order (priority × effort)

1. **C2** (missing OG images) — 15 min: switch to `/og` route
2. **C3** (add saas-readiness to sitemap) — 5 min
3. **C5, C6** (noindex docs/offline/demo) — 10 min
4. **C1, M3** (filter newsletter by status) — 20 min
5. **C4** (add RelatedBlogPostsSection to BlogArticle) — 2-4 hours; highest SEO leverage
6. **H1** (tighten blog descriptions) — 3-4 hours manual pass over 74 posts, or batch LLM rewrite
7. **H2, H8** (shorten long titles) — 1 hour
8. **H5** (technology page description) — 1 hour
9. **H11** (remove llms.txt slice cap) — 2 min
10. **M1** (add twitter creator to hubs) — 20 min
11. Remaining Medium/Low items as capacity allows

---

**Handoff:** seo-implementer for fix execution. Recommend opening a single PR for critical items (C1-C6) and a second PR for H-level metadata cleanup.

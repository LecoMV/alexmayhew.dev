# Files to copy to git worktrees for parallel Claude sessions
# These are typically gitignored files needed for the dev environment

# Environment variables
.env
.env.local
.env.development
.env.development.local
.dev.vars

# Local Claude settings
**/.claude/settings.local.json

# IDE settings that may have local overrides
.vscode/settings.json

# Node module caches (optional - speeds up npm install)
# node_modules/.cache
